services:
  web:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - signal-data:/app/data
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
volumes:
  signal-data:
